## Here, we learn operations that we can perform in git
 1. Cherry-Picking
 Applying specific commits from one branch to another branch without merging full history or, the branch
Steps: 
git clone <repo>
cd repo
git checkout <base-branch>
git checkout -b <new-branch or needed branch>
git cherry-pick <commit-id>
##Resolve the conflicts using
vi <file name>
git status
git add
git cherry-pick --continue
git commit --amend   ##remove old things/tickets and add your details
git push origin <new-branch>
git push origin HEAD:refs/heads/idcevo-pu2511-timeout-to-12hrs   ##If you forgot to checkout to new branch##


2. git pull -->> git pull = git fetch + git merge
(i) git fetch - downloads changes from the remote
(ii) git merge - merges the changes in your local branch

3. git merge <branch-name> ->> merges the <branch-name> into your current branch

4. git rebase - used to move or, combine a sequence of commits to a new base commit.
git rebase <upstream>

5. Squashing
git clone <repo>
cd repo
git checkout <ECU branch on top of which you want to cherry-pick>
git checkout -b <new-branch>
git cherry-pick <commit-id>
git cherry-pick <commit-id>
git cherry-pick <commit-id>
...
#resolve the conflicts if any#
git rebase -i HEAD~n #say you want to squash last n commits
#it opens interactive rebase
#pick the 1st commit/the commit in which you want to squash the changes
#squash or s the other commits
#change the commit msg if needed
git commit --amend
git push -f origin HEAD:refs/heads/<branch-name>

###Easier way to squash - 
git clone <repo>
cd repo
git checkout <ECU branch on top of which you want to cherry-pick>
git checkout -b <new-branch>
**cherry-pick all commits in a single go - 
git cherry-pick --no-commit <commit-id>
git cherry-pick --no-commit <commit-id>
git cherry-pick --no-commit <commit-id>
git cherry-pick --no-commit <commit-id>
git cherry-pick --no-commit <commit-id>
git cherry-pick --no-commit <commit-id>
git status
git add .
git commit (modify the commit)
git commit --amend (cross-check)
git push origin HEAD:refs/heads/<new-branch>

